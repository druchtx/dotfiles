export EDITOR='code'

export PATH="$HOME/.local/bin:./bin:/usr/local/bin:/usr/local/sbin:$ZSH/bin:$PATH"
export MANPATH="/usr/local/man:/usr/local/git/man:$MANPATH"

# your project folder that we can `c [tab]` to
# Note: when modifying WORK, also update functions/c and functions/_c accordingly
export WORK="$HOME/Workspace"
# Note: PLAYGROUNDS is derived from WORK, update accordingly if WORK changes
export PLAYGROUNDS="$WORK/self-taught"
