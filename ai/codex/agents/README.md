# Codex Agents

This directory is for Codex-specific agent definitions.

Use `agents/` for role-based specialists that should be explicitly
invoked, such as:

- `db-architect`
- `api-designer`
- `reviewer`

Do not put general process guidance here. Shared methods, checklists,
and reusable workflows belong under `ai/shared/skills/`.
